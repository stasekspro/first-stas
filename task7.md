# Тестовое задание №7: Абстракция и Page Object Pattern

## Цель задания
Познакомиться с **абстракцией** через рефакторинг тестов с использованием паттерна Page Object Model (POM).

## Что такое абстракция?
**Абстракция** — это выделение общих характеристик и поведения объектов. Мы создаем базовый класс с общей логикой, а детали реализуем в наследниках.

**Простой пример из жизни:**
- Есть абстрактное понятие "Транспорт" (имеет колеса, двигатель, может ехать)
- От него наследуются конкретные: "Машина", "Мотоцикл", "Грузовик"
- У всех есть общие свойства, но каждый едет по-своему

---

## Задачи

### Шаг 1: Создать базовый абстрактный класс страницы

Создай **абстрактный класс** `BasePage`:

```java
public abstract class BasePage {
    protected WebDriver driver;
    protected WebDriverWait wait;
    
    // Создай конструктор который принимает WebDriver driver
    // Инициализирует this.driver и this.wait (с таймаутом 10 секунд)
    
    // Абстрактные методы:
    public abstract String getPageUrl();
    public abstract boolean isPageLoaded();
}
```

---

### Шаг 2: Создать базовый класс для элементов страницы

Создай **абстрактный класс** `BaseElement`:

```java
public abstract class BaseElement {
    protected WebDriver driver;
    protected WebDriverWait wait;
    protected By locator;
    protected String name;
    
    // Создай конструктор который принимает: WebDriver driver, String locator, String name
    // Инициализирует все поля:
    // - this.driver = driver
    // - this.wait с таймаутом 10 секунд
    // - this.locator = By.cssSelector(locator)
    // - this.name = name
    
    // Создай методы:
    public boolean isDisplayed()
    public void waitUntilVisible()
    public String getText()
    protected WebElement getElement()
}
```

**Что здесь происходит?**
- `BaseElement` — это "скелет" для всех элементов
- Конструктор принимает: `driver`, `локатор (строка)`, `имя элемента`
- Локатор автоматически конвертируется в `By.cssSelector(locator)`

---

### Шаг 3: Создать класс Button

Создай класс `Button`, который наследуется от `BaseElement`:

```java
public class Button extends BaseElement {
    // Создай конструктор который принимает: WebDriver driver, String locator, String name
    // Вызывает super(driver, locator, name)
    
    // Создай метод click()
}
```

**Пример использования:**
```java
Button cartButton = new Button(chrome, "a.b-top-profile__cart", "Кнопка корзины");
cartButton.click();
```

---

### Шаг 4: Создать Page Object для главной страницы

Создай класс `HomePage`, который наследуется от `BasePage`:

```java
public class HomePage extends BasePage {
    private Button cartButton;
    
    // Создай конструктор который принимает WebDriver driver
    // Вызывает super(driver)
    // Инициализирует cartButton = new Button(driver, "a.b-top-profile__cart", "Иконка корзины")
    
    // Переопредели методы getPageUrl() и isPageLoaded() используя @Override
    // URL: "https://www.onliner.by/"
    // isPageLoaded проверяет элемент ".b-top-navigation"
    
    public CartPage openCart() {
        // Кликнуть на cartButton
        // Подождать URL "cart.onliner.by"
        // Вернуть new CartPage(driver)
    }
}
```

---

### Шаг 5: Создать Page Object для страницы корзины

Создай класс `CartPage` по аналогии с `HomePage`:

```java
public class CartPage extends BasePage {
    // Переопредели методы с @Override
    // URL: "https://cart.onliner.by/"
    // isPageLoaded проверяет URL содержит "cart.onliner.by"
    // Метод isCartFormDisplayed() проверяет элемент ".cart-form"
}
```

---

### Шаг 6: Рефакторинг теста

Замени код своего теста на этот:

```java
@Test
public void openCartPageWhenCartIconClicked() {
    HomePage homePage = new HomePage(chrome);
    CartPage cartPage = homePage.openCart();
    
    assertTrue(cartPage.isPageLoaded(), "Страница корзины не загрузилась");
    assertTrue(cartPage.isCartFormDisplayed(), "Форма корзины не отображается");
}
```

**Посмотри как тест стал легко читаться!**

Никаких локаторов в тесте — всё спрятано в Page Objects.

---

## Критерии приемки

✅ Создан абстрактный класс `BasePage`  
✅ Создан абстрактный класс `BaseElement`  
✅ Создан класс `Button`, наследующийся от `BaseElement`  
✅ Создан класс `HomePage`, наследующийся от `BasePage` и использующий `Button`  
✅ Создан класс `CartPage`, наследующийся от `BasePage`  
✅ Тест `openCartPageWhenCartIconClicked` работает

---

## Вопросы для самопроверки

1. Что такое абстракция?
2. Зачем нужен `BasePage`?
3. Зачем нужен `BaseElement`?
4. Можно ли создать объект `new BasePage(driver)` или `new BaseElement(driver, "selector", "name")`?
5. Что делает `super()` в конструкторе?
6. Почему методы нужно переопределять с `@Override`?

---

## Подсказки

💡 **Абстракция помогает:**
- Не дублировать код
- Сделать структуру понятной
- Легко добавлять новые элементы

💡 **Если изменится способ ожидания элементов, сколько мест надо поменять?**

---
